#####################################################
# 첫 컬럼을 인덱스로 사용하여 값의 합을 구하고 출력 #
#####################################################
cat sample.txt | awk '{ array[$1] += $2 } END {for (i in array) { print i, array[i] } }'
